const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    extend: {
      colors: {
        'white-a40': 'rgba(255, 255, 255, 0.4)',
        'purple-a50': 'rgba(128, 90, 213, 0.5)'
      },
    }
  }
}
